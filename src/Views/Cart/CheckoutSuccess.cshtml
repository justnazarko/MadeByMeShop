@{
    ViewData["Title"] = "Дякуємо за покупку";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center py-5">
            <div class="success-container p-5 bg-white rounded shadow-sm">
                <div class="success-icon mb-4">
                    <i class="fas fa-check-circle text-success" style="font-size: 5rem;"></i>
                </div>
                <h1 class="display-4 mb-4 text-success">Дякуємо за покупку!</h1>
                <p class="lead mb-4">Ваше замовлення успішно оформлено та прийнято до обробки.</p>
                <div class="order-details mb-4">
                    <p class="text-muted">Ми надіслали підтвердження на вашу електронну пошту.</p>
                    <p class="text-muted">Наш менеджер зв'яжеться з вами найближчим часом.</p>
                </div>
                <div class="action-buttons">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg px-5 me-3">
                        <i class="fas fa-home me-2"></i>На головну
                    </a>
                    <a asp-controller="Account" asp-action="Orders" class="btn btn-outline-primary btn-lg px-5">
                        <i class="fas fa-shopping-bag me-2"></i>Мої замовлення
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .success-container {
        background: linear-gradient(to bottom, #ffffff, #f8f9fa);
        border: 1px solid #e9ecef;
        transition: transform 0.3s ease;
    }

    .success-container:hover {
        transform: translateY(-5px);
    }

    .success-icon {
        animation: scaleIn 0.5s ease-out;
    }

    @keyframes scaleIn {
        0% {
            transform: scale(0);
            opacity: 0;
        }
        100% {
            transform: scale(1);
            opacity: 1;
        }
    }

    .action-buttons .btn {
        transition: all 0.3s ease;
    }

    .action-buttons .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
</style>