@model MadeByMe.src.Models.ApplicationUser

@{
    ViewData["Title"] = "Ваш профіль";
}

<h2>Профіль користувача</h2>

<div class="card mt-3 p-3">
    <dl class="row">

        <img src="@(Model.ProfilePicture ?? "/images/default-avatar.png")" class="card-img-top" alt="Аватар">

        <dt class="col-sm-3">Ім’я</dt>
        <dd class="col-sm-9">@Model.UserName</dd>

        <dt class="col-sm-3">Електронна пошта</dt>
        <dd class="col-sm-9">@Model.Email</dd>

        <dt class="col-sm-3">Номер мобільного</dt>
        <dd class="col-sm-9">@Model.PhoneNumber</dd>
    </dl>

    <a asp-action="EditProfile" asp-controller="Account" class="btn btn-primary">Редагувати профіль</a>

    @if (User.IsInRole("Seller"))
    {
        <p>Ви продавець!</p>
    }
    else
    {
        <form asp-action="BecomeSeller" method="post">
            <button type="submit" class="btn btn-warning">Стати продавцем</button>
        </form>
    }


</div>


@* <div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <img src="@(Model.ProfilePicture ?? "/images/default-avatar.png")" class="card-img-top" alt="Аватар">
                <div class="card-body text-center">
                    <h5 class="card-title">@Model.UserName</h5>
                    <p class="text-muted">@Model.Email</p>
                    <a asp-action="UpdateProfile" class="btn btn-outline-primary">Редагувати профіль</a>
                </div>
            </div>
        </div>
        <div class="col-md-8">
            <h3>Ваші замовлення</h3>
            <!-- Тут можна додати історію замовлень -->
        </div>
    </div>
</div> *@